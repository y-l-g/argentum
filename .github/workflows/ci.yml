name: CI

on:
  push:
    branches: [master]
  pull_request:

concurrency:
  group: ci-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings

# Topcoat and Toasty are git dependencies (branch `main`) pinned by the
# committed Cargo.lock — nothing to clone here. Bump them deliberately with
# `cargo update -p topcoat` / `cargo update -p toasty`.

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: rustup update stable && rustup default stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --workspace

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: rustup update stable && rustup default stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --workspace --all-targets -- -D warnings

  fmt:
    name: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: rustup update stable && rustup default stable
      - uses: Swatinem/rust-cache@v2
      # Explicit `-p` list: deterministic and independent of what cargo has
      # checked out; `cargo fmt` only ever touches workspace members, so this
      # covers every crate this repo owns.
      - run: >-
          cargo fmt -p argentum-core -p argentum-macros -p argentum-ui
          -p showcase -p xtask -- --check
      - name: Install topcoat CLI (for `topcoat fmt`)
        run: |
          cargo install --git https://github.com/tokio-rs/topcoat --branch main topcoat-cli --locked
      - name: topcoat fmt
        run: |
          topcoat fmt
          git diff --exit-code
