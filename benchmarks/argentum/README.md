# Storefront: Argentum

The Argentum (Topcoat-based) app under the benchmark harness. Stub: serves a
single page through the toolkit's `view!` pipeline.

```sh
cargo run
```

This app is detached from the root workspace on purpose (see `../README.md`).
